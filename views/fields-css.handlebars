.showtags span:before, .showtags a:before, .showtags span:after, .showtags a:after {
    font-size: smaller;
    color: #585;
    font-weight: bold;
}

{{#each fields}}
    .showtags .{{schema}}_{{field}}:before {
        content: "<{{label}}>";
    }

    .showtags .{{schema}}_{{field}}:after {
        content: "</{{label}}>";
    }

    .{{schema}}_{{field}} {
        {{#if link}}
            color: inherit;
        {{/if}}
        {{css}}
    }
{{/each}}
